spring:
  application:
    name: product
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      profile: dev

  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
logging:
  level:
    org.springframework.cloud.netflix: debug
